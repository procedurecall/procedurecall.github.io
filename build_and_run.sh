bundle exec jekyll serve
echo "\nEnter _site directory and push contents to gh-pages branch to push to production website (delete and pull that branch as _site directory first if on new machine, before re-building)"
